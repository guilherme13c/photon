services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.3.2
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    volumes:
      - ./zookeeper_data:/var/lib/zookeeper/data
      - ./zookeeper_log:/var/lib/zookeeper/log

  kafka:
    image: confluentinc/cp-kafka:7.3.2
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: 'zookeeper:2181'
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
    volumes:
      - ./kafka_data:/var/lib/kafka/data

  kafka-exporter:
    image: danielqsj/kafka-exporter:latest
    command: ["--kafka.server=kafka:29092"]
    depends_on:
      - kafka

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    privileged: true
    devices:
      - /dev/kmsg:/dev/kmsg
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /dev/disk:/dev/disk:ro
    command:
      - --docker_only=true

  init-kafka:
    image: confluentinc/cp-kafka:7.3.2
    depends_on:
      - kafka
    entrypoint: [ '/bin/sh', '-c' ]
    command: |
      set -eu
      until kafka-topics --bootstrap-server kafka:29092 --list > /dev/null 2>&1; do
        echo 'Waiting for Kafka to become reachable...'
        sleep 2
      done

      echo 'Creating Kafka topics'
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic fetched-pages --replication-factor 1 --partitions 3
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic cleaned_documents --replication-factor 1 --partitions 3
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic urls --replication-factor 1 --partitions 3
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic frontier-dlq --replication-factor 1 --partitions 1
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic fetcher-dlq --replication-factor 1 --partitions 1
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic extractor-dlq --replication-factor 1 --partitions 1
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic embedder-dlq --replication-factor 1 --partitions 1
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic dynamic-urls --replication-factor 1 --partitions 3
      
      echo 'Successfully created the following topics:'
      kafka-topics --bootstrap-server kafka:29092 --list

  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
      MINIO_PROMETHEUS_AUTH_TYPE: public
    command: server /data --console-address ":9001"
    volumes:
      - ./minio_data:/data

  init-minio:
    image: minio/mc:latest
    depends_on:
      - minio
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    entrypoint: >
      /bin/sh -c "
      until (/usr/bin/mc alias set myminio http://minio:9000 $${MINIO_ROOT_USER} $${MINIO_ROOT_PASSWORD}) do echo '...waiting...' && sleep 1; done;
      /usr/bin/mc mb myminio/html-payloads || true;
      /usr/bin/mc anonymous set public myminio/html-payloads;
      exit 0;
      "

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_data:/qdrant/storage

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - ./redis_data:/data

  redis-exporter:
    image: oliver006/redis_exporter:latest
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis

  frontier:
    build:
      context: ./frontier
    depends_on:
      - kafka
      - redis
    environment:
      KAFKA_BROKERS: kafka:29092
      REDIS_URL: redis://redis:6379
      PORT: 8080
      KAFKA_DYNAMIC_URLS_TOPIC: dynamic-urls
    ports:
      - "8080:8080"
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

  extractor:
    build:
      context: ./extractor
    depends_on:
      - kafka
      - minio
    environment:
      KAFKA_BROKERS: kafka:29092
      MINIO_ENDPOINT: http://minio:9000
      KAFKA_DLQ_TOPIC: extractor-dlq
      PROMETHEUS_PORT: ${EXTRACTOR_PROMETHEUS_PORT:-8001}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

  embedder:
    build:
      context: ./embedder
    depends_on:
      - kafka
      - qdrant
      - minio
    environment:
      KAFKA_BROKER: kafka:29092
      QDRANT_URL: http://qdrant:6333
      QDRANT_COLLECTION_NAME: photon_documents
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      KAFKA_DLQ_TOPIC: embedder-dlq
      NUM_WORKERS: 1
      PROMETHEUS_PORT: ${EMBEDDER_PROMETHEUS_PORT:-8000}
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G


  renderer:
    build:
      context: ./renderer
    depends_on:
      - kafka
      - minio
    environment:
      KAFKA_BROKER: kafka:29092
      KAFKA_TOPIC: dynamic-urls
      KAFKA_PRODUCER_TOPIC: fetched-pages
      KAFKA_GROUP: renderer-group
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "3000:3000"
    deploy:
      resources:
        limits:
          memory: 2G

  fetcher:
    build:
      context: ./fetcher
    depends_on:
      - kafka
      - frontier
      - renderer
      - minio
    environment:
      KAFKA_BROKER: kafka:29092
      KAFKA_TOPIC: urls
      KAFKA_PRODUCER_TOPIC: fetched-pages
      KAFKA_DYNAMIC_URLS_TOPIC: frontier-ingest
      KAFKA_GROUP: fetcher-group
      RENDERER_URL: http://renderer:3000
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      KAFKA_DLQ_TOPIC: fetcher-dlq
      PROMETHEUS_PORT: ${FETCHER_PROMETHEUS_PORT:-2112}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  # Deliberately isolated behind the test profile. It provides stable static,
  # dynamic, redirect, robots, and timestamp-recording endpoints for the
  # acceptance suite without ever crawling the public web.
  origin:
    build:
      context: ./tests/origin
    profiles: ["test"]
    ports:
      - "18088:8088"

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    depends_on:
      - prometheus
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_INSTALL_PLUGINS=yesoreyeram-infinity-datasource
    volumes:
      - ./config/grafana/provisioning:/etc/grafana/provisioning
      - ./config/grafana/dashboards:/var/lib/grafana/dashboards
